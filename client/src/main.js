const gridSize = 20;

function createGrid(rootEl) {
  for (const x = 1; x <= gridSize; x++) {
    const rowEl = document.createElement("div");

    rootEl.
  }
}

document.addEventListener("load", function () {
  const rootEl = document.getElementById("root");

  

});
